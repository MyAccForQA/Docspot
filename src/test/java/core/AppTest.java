package core;

import org.junit.Assert;
import org.junit.Test;

public class AppTest {
	
	@Test
	public void test() {		
		// Assert.assertEquals(message, expected, actual);
		Assert.assertEquals("Error", "2", "2");
	}

}
